# Hermes Gateway URL (default: http://localhost:8642)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8642

# Hermes API Server Key — set this to your API_SERVER_KEY in ~/.hermes/.env
NEXT_PUBLIC_API_KEY=

# Optional: bind Studio to a non-loopback host
# HOST=0.0.0.0
